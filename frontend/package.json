{
  "name": "frontend",
  "private": true,
  "version": "0.0.0",
  "scripts": {
    "dev": "vite",
    "build": "vue-tsc --noEmit && vite build",
    "build:local": "vue-tsc --noEmit && vite build --outDir ../backend/public --emptyOutDir",
    "preview": "vite preview"
  },
  "dependencies": {
    "@vuetter/vite-plugin-vue-svg": "^0.1.2",
    "browser-image-compression": "^1.0.17",
    "sass": "^1.49.7",
    "vue": "^3.2.25",
    "vue-router": "4",
    "vue-tsc": "^0.31.4"
  },
  "devDependencies": {
    "@types/vue-router": "^2.0.0",
    "@vitejs/plugin-vue": "^2.2.0",
    "typescript": "^4.5.5",
    "vite": "^2.8.0"
  }
}
